<%= form_for ([@conversation, @message]) do |f| %>

<div><%= f.label :body %></div>
<div><%= f.text_field :body %></div>

<div><%= f.label :user_id %></div>
<div><%= f.text_field :user_id %></div>

<%= f.submit "Submit" %>

<% end %>
